<form method="post" action="{{ path('app_local_delete', {'id': local.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ local.id) }}">
    <button class="btn">Delete</button>
</form>
