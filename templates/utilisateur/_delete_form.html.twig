<form method="post" action="{{ path('app_utilisateur_delete', {'id': utilisateur.id}) }}">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.id) }}">
    <button class="btn">Delete</button>
</form>
